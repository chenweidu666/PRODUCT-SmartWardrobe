# 复制为 .env 后由 docker compose 自动加载（与本目录 docker-compose.yml 同目录）
# cp .env.example .env

JWT_SECRET=请改为长随机串

# 可选：留空则 Docker 生产环境允许任意 Origin（仅可信网络）。公网部署请写白名单：
# CORS_ORIGINS=http://localhost:8080,http://192.168.31.10:8080
