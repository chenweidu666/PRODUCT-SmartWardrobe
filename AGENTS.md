# AGENTS.md — WEB-Smart-Wardrobe-System

## 仓库定位

SmartWardrobe 智能衣橱系统。

GitHub：`chenweidu666/WEB-Smart-Wardrobe-System`  
本地：`1-github-仓库备份/` 下对应分类目录。

---

## 0. Git 提交（强制）

**只要修改了仓库内文件，就必须立刻提交代码。**

| 规则 | 说明 |
|------|------|
| 改完即提交 | 每次完成一轮有意义的代码/配置/文档改动后，立即 `git add` + `git commit`，不要攒到最后 |
| 不要等用户催 | 用户无需再说「提交」；修改完成后默认提交 |
| 提交信息 | 1–2 句说明「为什么」，遵循仓库现有风格；用 HEREDOC 传 message |
| 禁止空提交 | 没有实际变更时不要 commit |
| 不擅自推送 | 仅本地 commit；`git push` 需用户明确要求 |
| 安全 | 不改 git config；不 force push main/master；不跳过 hooks；密钥类文件不提交 |

适用：本仓库内的一切改动。


---

## 1. 通用约定

- 目录优先使用 `01-` / `02-` … 编号前缀（与家族仓库一致）
- **禁止入库**：`.env`、API Key、证书私钥、大体量权重/数据集/媒体、单文件 >50MB
- 保持现有命名与文档风格；不擅自大范围重构无关目录
- 先读本文件与根 `README.md`，再动手改

## 2. Agent 工作方式

1. 明确任务范围，优先最小改动
2. 代码/文档改完后按 §0 提交
3. 若涉及跨仓库路径，核对 `1-github-仓库备份/` 下实际目录（`AI/`、`RES/`、`DOCS/`、`EMBEDDED/` 等）


## 3. 本仓库要点

- 前后端约定见 README；密钥与用户数据勿入库
